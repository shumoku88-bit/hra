with ALedger.Household; use ALedger.Household;

package ALedger.TUI is

   --  ========================================================================
   --  Ada Native Interactive Terminal UI (Zero External Dependencies)
   --  ========================================================================

   procedure Run_Interactive_TUI (State : in out Household_State);

end ALedger.TUI;
